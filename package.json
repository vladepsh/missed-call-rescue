{
  "name": "missed-call-rescue",
  "version": "1.0.0",
  "private": true,
  "description": "Safety-gated missed-call recovery demo for locksmith and home-service teams.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "lint": "node --check server.js && node --check public/app.js",
    "scan": "node scripts/scan-sensitive.js",
    "test:unit": "node --test tests/server.test.js",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e",
    "check": "npm run lint && npm run scan && npm test",
    "screenshots": "node scripts/capture-screenshots.js"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
